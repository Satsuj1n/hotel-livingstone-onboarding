# Plan M1.1 — Setup ambiente + estrutura base do Site

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:subagent-driven-development` (recommended) or `superpowers:executing-plans` to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Liferay 7.4 GA132 rodando localmente na **porta 8081** com site "Hotel Livingstone" criado via UI, contendo 6 pages (Home, Quartos, Restaurante, Eventos, Sobre, Contato), navegação principal funcionando e multi-idioma PT-BR/EN-US configurado. Documentação base (README + ROADMAP) e ADR-001 commitados.

**Architecture:** Workspace Liferay nativo (não Docker no M1) numa subpasta `workspace/`. Configuração de site/pages/i18n via UI Liferay (Control Panel) — não vai pra git, fica no DB do Liferay. Documentação versionada em `docs/`. Bundle Liferay (`workspace/bundles/`) gitignored — re-baixável via `blade server init`.

**Tech Stack:** Liferay 7.4 CE GA132, Blade CLI 8.0.0, Java 21 (Liberica), Gradle (via wrapper), Markdown, Git.

**Posição no projeto:** Parte 1 de 3 do M1 (M1.1 = setup+site base; M1.2 = tema+content+fragments; M1.3 = polish+retrospectiva+slides+apresentação 25/05).

**Pré-requisitos:**
- Repo `hotel-livingstone-onboarding` inicializado (✅ já feito, hash `aba01a2`)
- Blade CLI 8.0.0 instalado (✅ verificado)
- Java 21 instalado (assumido, validar na Task 3)
- ~500MB de disco livre pro bundle Liferay
- Porta 8081 livre (não usada pelo GDF nem outro processo)

---

## Estrutura de arquivos que este plan cria

```
hotel-livingstone-onboarding/
├── README.md                                          (Task 1)
├── ROADMAP.md                                         (Task 1)
├── .gitignore                                         (Task 3)
├── docs/
│   ├── adr/
│   │   └── ADR-001-workspace-nativo-no-m1.md         (Task 2)
│   └── learning-log/
│       ├── 2026-04-26-setup-inicial.md               (Task 1, 2, 3)
│       ├── 2026-04-XX-liferay-rodando.md             (Task 4, 5)
│       └── 2026-04-XX-site-criado.md                 (Task 6, 7, 8, 9)
└── workspace/                                         (Task 3 — gerado pelo blade)
    ├── bundles/                                       (Task 4 — gitignored)
    │   └── tomcat-9.0.x/
    │       └── conf/
    │           └── server.xml                         (Task 4 — porta 8081, gitignored)
    ├── modules/
    ├── themes/
    ├── configs/
    ├── settings.gradle
    ├── gradle.properties
    └── ... (outros arquivos do template Liferay Workspace)
```

**Nota crítica:** configurações de site/pages/navegação/i18n feitas via Control Panel ficam no DB do Liferay, **não no git**. Pra reproduzibilidade do M1.1, esse plan + screenshots em learning-log + ROADMAP atualizado servem como spec re-executável manualmente. Em M2/M3 vamos introduzir Site Initializer pra versionar configurações.

---

## Convenções deste plan

- **Branches:** trabalhar em `main` direto (projeto solo, sem code review formal). Branches `feat/*` opcionais quando uma task for grande (>1 dia).
- **Commits PT-BR + Conventional Commits:** `docs:`, `feat:`, `fix:`, `config:`, `chore:`.
- **Learning-log obrigatório:** ao final de cada task (ou cada sessão de trabalho), criar/atualizar entrada em `docs/learning-log/YYYY-MM-DD-<titulo>.md` com: O que tentei / O que errei / O que firmou / Dúvida aberta. ~20 linhas.
- **Critério "ficou bom":** task só é `[x]` quando você consegue explicar o que fez sem consultar Claude.

---

## Task 1: README + ROADMAP iniciais (doc-first)

**Por que primeiro:** Doc-first é o pilar. Antes de qualquer código/config, escreve o que vai construir e o caminho. Serve como spec executável e como página de entrada do repo no GitHub.

**Files:**
- Create: `README.md`
- Create: `ROADMAP.md`
- Create: `docs/learning-log/2026-04-26-setup-inicial.md`

- [ ] **Step 1: Criar `README.md`**

```markdown
# Hotel Livingstone — Onboarding Liferay SEA

Projeto pessoal de onboarding na SEA Tecnologia cobrindo os desafios backend Liferay de 30/60/90 dias + trilha de carreira Junior I/II/III + Pleno I/II.

## O que é

Implementação evolutiva do site fictício "Hotel Livingstone" em Liferay 7.4 CE GA132. Cada milestone adiciona uma camada técnica nova:

- **M1 (até 25/05/2026):** Site funcional via UI (Pages, Web Content, Fragments, Theme, Navigation, multi-idioma PT/EN)
- **M2 (até 29/06/2026):** Workflow Kaleo + dynamic content + migração pra Docker Compose
- **M3 (até 27/07/2026):** Service Builder + REST Builder + tests + CI/CD GitLab + Workshop testes (gravado) + Liferay Objects + Diagnóstico real

## Stack

- Liferay 7.4 CE GA132
- Java 21 (Liberica)
- Blade CLI 8.0.0
- Gradle (via wrapper)
- Docker Compose (M2+)

## Status

🚧 **M1.1 em execução** (setup + site base) — apresentação M1 marcada pra 25/05/2026.

## Como rodar

```bash
cd workspace/
blade server init    # primeira vez — baixa bundle ~500MB
# Editar bundles/tomcat-*/conf/server.xml e trocar port="8080" por port="8081"
blade server start
# Aguarde ~15s e abra http://localhost:8081
```

## Estrutura do repo

Ver [ROADMAP.md](./ROADMAP.md) e [docs/specs/2026-04-26-design.md](./docs/specs/2026-04-26-design.md) pra detalhes completos.

## Convenções

- Docs em PT-BR; código em inglês; commits PT-BR Conventional Commits
- ADRs em `docs/adr/` com estrutura: Contexto → Opções consideradas → Trade-offs → Decisão → Consequências
- Learning log por sessão em `docs/learning-log/`

## Autor

Felipe Lima (felipenehz2003@gmail.com) — backend dev na SEA Tecnologia desde abril/2026.
```

- [ ] **Step 2: Criar `ROADMAP.md`**

```markdown
# Roadmap — Hotel Livingstone

Cronograma e checkpoints dos 3 milestones do projeto. Datas contratuais (apresentações marcadas pelo RH).

| Milestone | Data | Cursos cobertos | Status |
|---|---|---|---|
| **M1** — Site via UI | 2026-05-25 | Liferay Fundamentals + Content Creator | 🚧 Em execução (M1.1) |
| **M2** — Workflow + Docker | 2026-06-29 | Liferay Content Management | ⏸️ Bloqueado por M1 |
| **M3** — SB + REST + Tests + Upgrade C | 2026-07-27 | Service Builder + REST Builder + Junior I/II/III + Pleno I/II via workshop | ⏸️ Bloqueado por M2 |

## M1 — Detalhamento

Sub-dividido em 3 fases internas:

### M1.1 — Setup + Site base ⬅️ ATUAL

| Task | Status | Commit |
|---|---|---|
| 1. README + ROADMAP iniciais | 🚧 | — |
| 2. ADR-001 workspace nativo no M1 | ⏸️ | — |
| 3. Inicializar Liferay Workspace | ⏸️ | — |
| 4. Baixar bundle + ajustar porta + iniciar Liferay | ⏸️ | — |
| 5. Setup admin + login | ⏸️ | — |
| 6. Criar site + 6 pages | ⏸️ | — |
| 7. Multi-idioma PT/EN | ⏸️ | — |
| 8. Navigation menu principal | ⏸️ | — |
| 9. Retrospectiva M1.1 + tag git | ⏸️ | — |

### M1.2 — Tema + Web Content + Fragments
A planejar após conclusão de M1.1.

### M1.3 — Polish + Retrospectiva M1 + Slides + Apresentação 25/05
A planejar após conclusão de M1.2.

## M2 — Visão geral

Migração workspace nativo → Docker Compose (Liferay + Postgres 14 + Elasticsearch 7.17). Workflow Kaleo customizado (Reserva → Aprovação → Notificação). Web Content Structures + Templates. Asset Publisher dinâmico.

## M3 — Visão geral

Service Builder (Room, Booking, Guest, Package + finders + validações TDD). REST Builder (`/rooms`, `/bookings`, `/availability`). Tests unit + integration. CI GitLab. Upgrade C: workshop testes gravado (Pleno I+II), Liferay Objects (Pleno III parcial), sessão diagnóstico real.

## Próximos passos imediatos

1. Concluir M1.1 (este plan)
2. Criar plan M1.2 (tema + content + fragments)
3. Criar repo no GitHub pessoal + `git remote add origin` + `git push -u origin main`
```

- [ ] **Step 3: Criar `docs/learning-log/2026-04-26-setup-inicial.md`**

```markdown
# 2026-04-26 — Setup inicial do projeto

## O que tentei

- Criar README.md + ROADMAP.md como ponto de partida do projeto seguindo princípio doc-first.

## O que errei

(preencher conforme aparecer)

## O que firmou

- Doc-first não é só burocracia: serve como spec executável que o próprio dev consulta enquanto trabalha.
- README do GitHub público é a página de entrada do portfolio — investir na clareza paga.

## Dúvida aberta

(preencher conforme aparecer)
```

- [ ] **Step 4: Validação manual**

Abrir os 3 arquivos no editor e ler de ponta a ponta. Critérios:
- README explica o projeto pra alguém que nunca ouviu falar
- ROADMAP tem datas, status, próximos passos claros
- Learning-log tá com placeholder honesto (preenche conforme aparece)

- [ ] **Step 5: Commit**

```bash
git -C ~/Documents/SEA/hotel-livingstone-onboarding add README.md ROADMAP.md docs/learning-log/2026-04-26-setup-inicial.md
git -C ~/Documents/SEA/hotel-livingstone-onboarding commit -m "docs: README, ROADMAP e learning-log inicial"
```

---

## Task 2: ADR-001 — workspace nativo no M1, Docker no M2

**Por que segundo:** Decisão fundadora do M1. Documentar agora evita questionamento depois ("por que não usei Docker?"). ADR é o instrumento pedagógico do projeto — força explicitar trade-offs.

**Files:**
- Create: `docs/adr/ADR-001-workspace-nativo-no-m1.md`

- [ ] **Step 1: Criar pasta `docs/adr/`**

```bash
mkdir -p ~/Documents/SEA/hotel-livingstone-onboarding/docs/adr
```

- [ ] **Step 2: Criar `docs/adr/ADR-001-workspace-nativo-no-m1.md`**

```markdown
# ADR-001 — Workspace Liferay nativo no M1, Docker Compose a partir do M2

| Campo | Valor |
|---|---|
| **Data** | 2026-04-26 |
| **Status** | Aceito |
| **Decisor** | Felipe Lima (projeto pessoal) |

## Contexto

O projeto Hotel Livingstone precisa de uma instância Liferay 7.4 CE GA132 rodando localmente pra desenvolvimento. O ambiente do desenvolvedor (macOS, Apple Silicon, Docker Desktop instalado) suporta múltiplas estratégias de deploy local.

Restrição importante: o workspace GDF (`~/Documents/SEA/dados-aberto-lf7_4-workspace/`) já roda Liferay nativo na porta 8080. O Hotel Livingstone precisa coexistir sem conflito.

## Opções consideradas

### Opção A: Workspace Liferay nativo (blade server init/start)

**Prós:**
- Setup mais rápido (1 comando após blade init)
- Performance native melhor que Docker no macOS (Apple Silicon ainda penaliza Docker x86 emulation)
- Logs e debug diretos via Tomcat embarcado
- Menor consumo de RAM (sem overhead de containerização)
- Mesmo padrão do GDF (consistência mental)

**Cons:**
- HSQLDB embarcado (não é production-like)
- Configuração de porta requer edição manual de `server.xml` do Tomcat
- Não simula ambiente de produção (Docker é mais próximo de prod)
- Não treina Docker (pilar de Production-readiness do projeto)

### Opção B: Docker Compose desde o dia 1

**Prós:**
- Production-readiness desde o início
- Postgres 14 (real) em vez de HSQLDB
- Reproduzível 100% (1 `docker-compose up`)
- Treina Docker desde a primeira hora

**Cons:**
- Liferay + Postgres + Elasticsearch no Docker Desktop macOS = ~6-8GB RAM, lento no boot
- Curso Liferay Fundamentals (M1) assume bundle local
- Setup inicial mais complexo (~3-4h vs ~30min nativo)
- Configuração via UI no Liferay Docker é mais difícil de exportar

### Opção C: Híbrido — nativo no M1, Docker no M2

**Prós:**
- Aprendizado por camadas (1 conceito novo por milestone)
- M1 foca em UI Liferay (alinhado com curso Fundamentals)
- M2 introduz Docker quando começa a ter sentido (workflow + dynamic content + Postgres)
- Distribui curva de aprendizado

**Cons:**
- Migração no meio do projeto (custo de transição)
- Configurações feitas via UI no M1 podem precisar ser refeitas no M2 (Site Initializer mitiga, mas adiciona trabalho)

## Trade-offs

A questão central é **velocidade de aprendizado vs. realismo de produção**.

- Opção A maximiza velocidade no curto prazo mas adia o aprendizado de Docker pra M3 (ou nunca)
- Opção B maximiza realismo mas pode atrapalhar o aprendizado dos fundamentos Liferay no M1
- Opção C distribui ambos no tempo, com custo de uma migração no meio

## Decisão

**Opção C — Híbrido.** No M1.1, M1.2 e M1.3, usar workspace Liferay nativo na porta 8081. A partir do M2, migrar pra Docker Compose com Postgres 14 + Elasticsearch 7.17.

## Consequências

### Positivas
- M1 inteiro foca em UI Liferay sem distração de container tooling
- Docker entra no M2 quando workflow + dynamic content já justificam Postgres real
- Cada milestone adiciona EXATAMENTE 1 camada técnica nova (princípio do roadmap)

### Negativas
- M2 vai começar com ~4-6h dedicadas a Docker setup (não-código)
- Site Hotel Livingstone configurado via UI no M1 vai precisar ser reproduzido no Docker no M2 — ou manualmente, ou via Site Initializer (decisão pra M2)

### Mitigações
- Documentar todas as configurações do M1 em screenshots no learning-log → re-execução manual no M2 fica viável
- Considerar Site Initializer no M2 pra tornar configuração reproduzível via código
```

- [ ] **Step 3: Validação manual**

Ler o ADR completo. Critérios:
- Você consegue defender a Opção C contra alguém propondo A ou B
- Trade-offs estão honestos (não tendenciosos pra justificar a decisão)
- Consequências negativas estão listadas (não escondidas)

- [ ] **Step 4: Atualizar learning-log do dia**

Adicionar ao `docs/learning-log/2026-04-26-setup-inicial.md`:

```markdown
## ADR-001 escrita

- Estrutura "Contexto → Opções → Trade-offs → Decisão → Consequências" força raciocinar contra a decisão antes de tomá-la
- A seção "Opções consideradas" é onde mora o aprendizado — defender por que NÃO escolheu B é mais útil que justificar C
```

- [ ] **Step 5: Commit**

```bash
git -C ~/Documents/SEA/hotel-livingstone-onboarding add docs/adr/ADR-001-workspace-nativo-no-m1.md docs/learning-log/2026-04-26-setup-inicial.md
git -C ~/Documents/SEA/hotel-livingstone-onboarding commit -m "docs: ADR-001 sobre workspace nativo no M1"
```

---

## Task 3: Inicializar Liferay Workspace via blade

**Por que terceiro:** Agora que doc + decisão fundadora estão no lugar, criar a estrutura técnica do workspace. Blade CLI gera a estrutura padrão Liferay Workspace.

**Files:**
- Create (via blade): `workspace/` com estrutura Liferay Workspace
- Create: `.gitignore` (raiz do repo)

- [ ] **Step 1: Validar Java 21 instalado**

```bash
java -version
```

Esperado: output mostrando Java 21.x.x (Liberica). Se não, instalar Liberica 21 antes de prosseguir.

- [ ] **Step 2: Inicializar workspace Liferay 7.4 dentro de `workspace/`**

```bash
cd ~/Documents/SEA/hotel-livingstone-onboarding/
blade init -v 7.4 workspace
```

Esperado: pasta `workspace/` criada com estrutura padrão (`bundles/`, `configs/`, `modules/`, `themes/`, `wars/`, `gradle.properties`, `settings.gradle`, `build.gradle`, `gradlew`, etc.)

- [ ] **Step 3: Verificar estrutura criada**

```bash
ls -la ~/Documents/SEA/hotel-livingstone-onboarding/workspace/
```

Esperado: ver `gradle.properties`, `settings.gradle`, `build.gradle`, `gradlew`, pastas `configs/`, `modules/`, `themes/`, `wars/`.

- [ ] **Step 4: Verificar `gradle.properties` define `liferay.workspace.product`**

```bash
grep liferay.workspace.product ~/Documents/SEA/hotel-livingstone-onboarding/workspace/gradle.properties
```

Esperado: linha tipo `liferay.workspace.product=portal-7.4-ga132` (versão pode ser diferente — ajustar pra GA132 se necessário).

Se não for GA132, editar `workspace/gradle.properties` e trocar pra `liferay.workspace.product=portal-7.4-ga132`.

- [ ] **Step 5: Criar `.gitignore` na raiz do repo**

Criar `~/Documents/SEA/hotel-livingstone-onboarding/.gitignore` com:

```gitignore
# Liferay bundle (re-baixável via blade server init, ~500MB)
workspace/bundles/

# Gradle
workspace/.gradle/
workspace/build/
**/build/
**/.gradle/

# IDEs
.idea/
*.iml
.vscode/
.project
.classpath
.settings/

# OS
.DS_Store
Thumbs.db

# Liferay logs e temp
**/logs/
**/temp/
**/work/
```

- [ ] **Step 6: Validar git status**

```bash
git -C ~/Documents/SEA/hotel-livingstone-onboarding status
```

Esperado: ver `.gitignore` novo + `workspace/` (mas sem `workspace/bundles/`, que está ignorado, embora a pasta ainda nem exista).

- [ ] **Step 7: Atualizar learning-log**

Adicionar ao `docs/learning-log/2026-04-26-setup-inicial.md`:

```markdown
## Workspace Liferay inicializado

- `blade init -v 7.4` gera template completo (Gradle wrapper + estrutura modules/themes/wars/configs)
- `liferay.workspace.product` em `gradle.properties` é o que define a versão Liferay que o `blade server init` vai baixar
- `.gitignore` exclui `bundles/` porque é re-baixável (~500MB, não faz sentido versionar)
```

- [ ] **Step 8: Commit**

```bash
git -C ~/Documents/SEA/hotel-livingstone-onboarding add .gitignore workspace/ docs/learning-log/2026-04-26-setup-inicial.md
git -C ~/Documents/SEA/hotel-livingstone-onboarding commit -m "feat: workspace Liferay 7.4 GA132 inicializado em workspace/"
```

---

## Task 4: Baixar bundle + ajustar porta + iniciar Liferay

**Por que quarto:** Workspace existe mas é casca vazia. Agora baixar o Liferay (bundle Tomcat embarcado), ajustar porta pra 8081 (evitar conflito GDF) e subir o servidor.

**Files:**
- Download (via blade): `workspace/bundles/tomcat-9.0.x/` (~500MB, gitignored)
- Modify: `workspace/bundles/tomcat-*/conf/server.xml` (porta 8080 → 8081, gitignored)

- [ ] **Step 1: Baixar bundle Liferay (~500MB)**

```bash
cd ~/Documents/SEA/hotel-livingstone-onboarding/workspace/
blade server init
```

Esperado: download progride, bundle aparece em `workspace/bundles/`. Pode levar 5-15 minutos dependendo da internet.

Validar que terminou:
```bash
ls workspace/bundles/
```
Esperado: pasta tipo `tomcat-9.0.83/` ou similar.

- [ ] **Step 2: Editar `server.xml` do Tomcat — trocar porta 8080 por 8081**

```bash
sed -i '' 's/port="8080"/port="8081"/g' ~/Documents/SEA/hotel-livingstone-onboarding/workspace/bundles/tomcat-*/conf/server.xml
```

Validar:
```bash
grep 'port="80' ~/Documents/SEA/hotel-livingstone-onboarding/workspace/bundles/tomcat-*/conf/server.xml
```
Esperado: ver linhas com `port="8081"` no HTTP connector. Não deve ver mais `port="8080"`.

**Importante:** essa edição é local (gitignored). Documentar no README como passo manual pós `blade server init`.

- [ ] **Step 3: Validar porta 8081 está livre**

```bash
lsof -nP -iTCP:8081 -sTCP:LISTEN
```

Esperado: nenhum output (porta livre). Se algo estiver usando, parar antes de iniciar Liferay.

- [ ] **Step 4: Iniciar Liferay em background**

```bash
cd ~/Documents/SEA/hotel-livingstone-onboarding/workspace/
blade server start
```

Esperado: comando retorna rapidamente (servidor sobe em background). O servidor leva ~30-60s pra estar realmente pronto.

- [ ] **Step 5: Aguardar Liferay subir + monitorar logs**

```bash
tail -f ~/Documents/SEA/hotel-livingstone-onboarding/workspace/bundles/tomcat-*/logs/catalina.out
```

Esperar mensagem tipo `Server startup in [N] milliseconds` ou `Initializing portal portalweb`. Pode levar 30-90s.

Quando aparecer, `Ctrl+C` pra sair do tail.

- [ ] **Step 6: Validar HTTP em localhost:8081**

```bash
curl -s -o /dev/null -w "HTTP %{http_code}\n" http://localhost:8081
```

Esperado: `HTTP 200` ou `HTTP 302` (redirect pra setup wizard).

- [ ] **Step 7: Atualizar README com nota sobre porta**

Editar `README.md`, na seção "Como rodar", confirmar que tem o comando do `sed` ou equivalente:

```bash
# Após blade server init (uma vez):
sed -i '' 's/port="8080"/port="8081"/g' workspace/bundles/tomcat-*/conf/server.xml
```

- [ ] **Step 8: Atualizar learning-log**

Criar `docs/learning-log/2026-04-XX-liferay-rodando.md` (ajuste data pra dia real):

```markdown
# YYYY-MM-DD — Liferay rodando localmente

## O que tentei

- Baixar bundle Liferay 7.4 GA132 via blade server init
- Mudar porta padrão de 8080 pra 8081 pra coexistir com GDF
- Subir Liferay e validar via curl

## O que errei

(preencher conforme aparecer — provavelmente: esquecer de mudar a porta antes do start, conflito de porta)

## O que firmou

- `blade server init` baixa bundle só uma vez; está em `workspace/bundles/` (gitignored)
- `server.xml` do Tomcat é onde a porta HTTP é configurada (não no Liferay portal-ext.properties)
- HTTP 200 ou 302 em `/` = Liferay subiu corretamente; primeiro acesso redireciona pro setup wizard

## Dúvida aberta

- Como portar a edição do `server.xml` pra Site Initializer ou config script (M2)?
```

- [ ] **Step 9: Commit (só docs e README — bundles gitignored)**

```bash
git -C ~/Documents/SEA/hotel-livingstone-onboarding add README.md docs/learning-log/
git -C ~/Documents/SEA/hotel-livingstone-onboarding commit -m "docs: bundle Liferay baixado e rodando na porta 8081"
```

---

## Task 5: Setup admin + login no Liferay

**Por que quinto:** Liferay subiu mas tá em estado "setup wizard". Configurar admin agora pra poder usar Control Panel nas tasks seguintes.

**Files:**
- Modify: `docs/learning-log/2026-04-XX-liferay-rodando.md`
- Create: `docs/credenciais.md` (gitignored — adicionar ao .gitignore)

- [ ] **Step 1: Adicionar `docs/credenciais.md` ao `.gitignore`**

Editar `.gitignore` e adicionar:

```gitignore
# Credenciais locais (nunca versionar)
docs/credenciais.md
```

- [ ] **Step 2: Abrir o setup wizard no browser**

```
http://localhost:8081
```

Liferay redireciona pra `/setup-wizard` na primeira execução.

- [ ] **Step 3: Preencher setup wizard**

- **Default Language:** Português (Brasil)
- **Default Time Zone:** America/Sao_Paulo
- **Administrator Name:** Felipe Lima
- **Administrator Email:** felipenehz2003@gmail.com
- **Database:** Default Database (HSQLDB) — manteremos no M1, migra pra Postgres no M2
- Aceitar termos e clicar em "Finish Configuration"

Aguardar processamento (~30s).

- [ ] **Step 4: Login com credencial inicial**

Liferay envia senha temporária pro email configurado, OU usa default `test` (depende do setup). Caso default: `test@liferay.com / test`.

- [ ] **Step 5: Trocar senha do admin**

Quando solicitado, definir senha forte. Anotar em `docs/credenciais.md`:

```markdown
# Credenciais locais (NUNCA commitar)

## Liferay 7.4 — Hotel Livingstone (porta 8081)

- URL: http://localhost:8081
- Admin email: felipenehz2003@gmail.com
- Admin password: <senha-forte-gerada>

## Outros

(preencher conforme aparecer)
```

Confirmar que `docs/credenciais.md` NÃO aparece em `git status` (deve estar gitignored).

- [ ] **Step 6: Validar acesso ao Control Panel**

No Liferay logado, clicar no ícone de hambúrguer (canto superior esquerdo) e navegar até `Control Panel → Sites`.

Esperado: ver lista de Sites (vazia ou só com "Liferay" default).

- [ ] **Step 7: Screenshot do Control Panel pro learning-log**

Tirar screenshot do Control Panel logado e salvar em `docs/learning-log/screenshots/2026-04-XX-control-panel.png` (criar pasta `screenshots/` se necessário).

- [ ] **Step 8: Atualizar learning-log**

Adicionar à entrada do dia:

```markdown
## Setup admin

- Setup wizard só aparece na primeira execução (depois fica gravado no DB)
- Senha default `test/test` é insegura mesmo em dev — trocar imediatamente
- HSQLDB no M1 é OK; Postgres entra no M2 pra suportar workflow + concurrent users
```

- [ ] **Step 9: Commit**

```bash
git -C ~/Documents/SEA/hotel-livingstone-onboarding add .gitignore docs/learning-log/
git -C ~/Documents/SEA/hotel-livingstone-onboarding commit -m "config: admin Liferay configurado + credenciais.md gitignored"
```

---

## Task 6: Criar site "Hotel Livingstone" + 6 pages

**Por que sexto:** Liferay configurado e logado. Agora criar o site real do projeto + estrutura de pages que o curso pede.

**Files:**
- Modify: `ROADMAP.md` (marcar Task 6 como done)
- Create: `docs/learning-log/2026-04-XX-site-criado.md`

- [ ] **Step 1: Criar Site "Hotel Livingstone" via Control Panel**

Navegar: `Control Panel → Sites → Sites → ➕ (Add) → Blank Site`

- **Name:** Hotel Livingstone
- **Site URL:** `/hotel-livingstone` (auto-gerado pelo nome)
- **Membership Type:** Open ou Restricted (escolher Open pra simplicidade — qualquer um pode visitar)

Clicar em "Save".

- [ ] **Step 2: Validar site criado**

Acessar: `http://localhost:8081/web/hotel-livingstone`

Esperado: página em branco do site (ainda não tem pages).

- [ ] **Step 3: Criar Page "Home"**

Dentro do site Hotel Livingstone: `Site Administration → Site Builder → Pages → ➕ (Add Public Page)`

- **Name:** Home
- **Type:** Widget Page (vazia, sem conteúdo)
- Clicar em "Save"

- [ ] **Step 4: Criar Pages restantes**

Repetir o passo 3 pra:
- Quartos
- Restaurante
- Eventos
- Sobre
- Contato

- [ ] **Step 5: Validar 6 pages criadas**

Acessar `http://localhost:8081/web/hotel-livingstone/home` (e variações pra cada page).

Esperado: cada URL retorna HTTP 200 com a page (vazia, mas existe).

```bash
for page in home quartos restaurante eventos sobre contato; do
  curl -s -o /dev/null -w "%{http_code} /web/hotel-livingstone/$page\n" "http://localhost:8081/web/hotel-livingstone/$page"
done
```

Esperado: 6 linhas com `200`.

- [ ] **Step 6: Tornar "Home" a page padrão do site**

`Site Administration → Site Builder → Pages → Home → Configure → Type → Set as Default Landing Page`

Validar:
```bash
curl -s -o /dev/null -w "%{http_code}\n" http://localhost:8081/web/hotel-livingstone
```
Esperado: `200` (renderiza Home como padrão).

- [ ] **Step 7: Screenshots dos 6 pages no learning-log**

Salvar em `docs/learning-log/screenshots/2026-04-XX-pages/`:
- `01-home.png`
- `02-quartos.png`
- `03-restaurante.png`
- `04-eventos.png`
- `05-sobre.png`
- `06-contato.png`

- [ ] **Step 8: Atualizar ROADMAP**

Em `ROADMAP.md`, marcar Task 6 como `✅` e adicionar commit hash quando feito.

- [ ] **Step 9: Criar learning-log entry**

Criar `docs/learning-log/2026-04-XX-site-criado.md`:

```markdown
# YYYY-MM-DD — Site Hotel Livingstone criado

## O que tentei

- Criar site Hotel Livingstone com 6 pages estruturadas (Home, Quartos, Restaurante, Eventos, Sobre, Contato)
- Configurar Home como landing page padrão

## O que errei

(preencher)

## O que firmou

- Site = container; Page = unidade de URL dentro do site
- "Widget Page" cria page vazia onde a gente põe widgets/portlets/fragments depois
- Default Landing Page é configurado por page (não no site)
- URL do site é `/web/<friendly-url>` — friendly URL é gerado a partir do nome do site

## Dúvida aberta

- Como versionar essa configuração de site/pages? (resposta provável: Site Initializer no M2)
```

- [ ] **Step 10: Commit**

```bash
git -C ~/Documents/SEA/hotel-livingstone-onboarding add ROADMAP.md docs/learning-log/
git -C ~/Documents/SEA/hotel-livingstone-onboarding commit -m "docs: site Hotel Livingstone criado com 6 pages"
```

---

## Task 7: Multi-idioma PT-BR / EN-US

**Por que sétimo:** Curso Fundamentals cobre i18n. Configurar agora deixa o site preparado pra conteúdo bilíngue (que entra no M1.2 com Web Content).

**Files:**
- Modify: `ROADMAP.md`
- Modify: `docs/learning-log/2026-04-XX-site-criado.md`

- [ ] **Step 1: Adicionar EN-US como idioma do site**

Navegar: `Site Administration → Configuration → Site Settings → General → Languages`

- **Default Language:** Português (Brasil) — já default
- **Active Languages:** marcar Português (Brasil) + English (United States)
- Clicar em "Save"

- [ ] **Step 2: Validar URLs com prefixo de idioma**

```bash
curl -s -o /dev/null -w "PT %{http_code} | " "http://localhost:8081/pt-BR/web/hotel-livingstone" && \
curl -s -o /dev/null -w "EN %{http_code}\n" "http://localhost:8081/en-US/web/hotel-livingstone"
```

Esperado: `PT 200 | EN 200`

- [ ] **Step 3: Configurar fallback comportamento (opcional)**

Validar default: se acessa sem prefixo, qual idioma retorna?

```bash
curl -s -I "http://localhost:8081/web/hotel-livingstone" | grep -i 'content-language\|location'
```

Esperado: deve retornar PT-BR por default.

- [ ] **Step 4: Screenshot da config de idiomas**

Salvar em `docs/learning-log/screenshots/2026-04-XX-pages/07-i18n-config.png`.

- [ ] **Step 5: Atualizar learning-log**

Adicionar ao `docs/learning-log/2026-04-XX-site-criado.md`:

```markdown
## Multi-idioma PT/EN

- Liferay suporta múltiplos idiomas via prefixo de URL (`/pt-BR/`, `/en-US/`)
- "Active Languages" no site control panel é onde marca quais idiomas o site aceita
- Translations de Pages são feitos por page (cada page tem labels traduzíveis)
- Default Language define o que retorna sem prefixo
```

- [ ] **Step 6: Atualizar ROADMAP**

Marcar Task 7 como `✅`.

- [ ] **Step 7: Commit**

```bash
git -C ~/Documents/SEA/hotel-livingstone-onboarding add ROADMAP.md docs/learning-log/
git -C ~/Documents/SEA/hotel-livingstone-onboarding commit -m "config: site Hotel Livingstone com PT-BR + EN-US"
```

---

## Task 8: Navigation menu principal

**Por que oitavo:** As 6 pages existem mas não têm menu visível ligando elas. Navigation menu = navegação superior do site.

**Files:**
- Modify: `ROADMAP.md`
- Modify: `docs/learning-log/2026-04-XX-site-criado.md`

- [ ] **Step 1: Criar Navigation Menu "Principal"**

Navegar: `Site Administration → Site Builder → Navigation Menus → ➕ (Add)`

- **Name:** Principal
- Clicar em "Save"

- [ ] **Step 2: Adicionar 6 itens (1 por page)**

Dentro do menu "Principal", clicar em "Add → Page" e selecionar cada uma das 6 pages:
1. Home
2. Quartos
3. Restaurante
4. Eventos
5. Sobre
6. Contato

Salvar a cada item.

- [ ] **Step 3: Marcar "Principal" como Primary Navigation**

`Configure → Mark this navigation as the site's primary navigation`

Isso faz o tema padrão do Liferay renderizar esse menu no topo das pages.

- [ ] **Step 4: Validar menu aparece no topo**

Acessar `http://localhost:8081/web/hotel-livingstone/home` no browser.

Esperado: ver menu superior com 6 items (Home, Quartos, Restaurante, Eventos, Sobre, Contato), todos clicáveis.

- [ ] **Step 5: Validar navegação entre pages**

Clicar em cada item do menu e verificar que navega corretamente.

- [ ] **Step 6: Screenshot do menu funcionando**

Salvar em `docs/learning-log/screenshots/2026-04-XX-pages/08-navigation-menu.png`.

- [ ] **Step 7: Atualizar learning-log**

Adicionar ao `docs/learning-log/2026-04-XX-site-criado.md`:

```markdown
## Navigation menu principal

- Navigation Menu é independente das Pages (uma Page pode estar em vários Menus, ou em nenhum)
- "Primary Navigation" é o menu que o tema padrão renderiza no topo
- Se o site tiver mais de 1 Navigation Menu, só 1 pode ser Primary
- Reordenar items no menu é drag-and-drop no Site Builder
```

- [ ] **Step 8: Atualizar ROADMAP**

Marcar Task 8 como `✅`.

- [ ] **Step 9: Commit**

```bash
git -C ~/Documents/SEA/hotel-livingstone-onboarding add ROADMAP.md docs/learning-log/
git -C ~/Documents/SEA/hotel-livingstone-onboarding commit -m "config: navigation menu Principal com 6 items"
```

---

## Task 9: Retrospectiva M1.1 + tag git

**Por que último:** Fechar M1.1 com retrospectiva curta (não precisa ser o doc gigante de M1 inteiro — esse vem em M1.3) + tag git pra marcar checkpoint.

**Files:**
- Create: `docs/retrospectivas/M1.1-setup-site-base.md`
- Modify: `ROADMAP.md`

- [ ] **Step 1: Criar `docs/retrospectivas/` se não existir**

```bash
mkdir -p ~/Documents/SEA/hotel-livingstone-onboarding/docs/retrospectivas
```

- [ ] **Step 2: Criar `docs/retrospectivas/M1.1-setup-site-base.md`**

```markdown
# Retrospectiva M1.1 — Setup ambiente + estrutura base do Site

| Campo | Valor |
|---|---|
| **Período** | 2026-04-XX a 2026-04-XX |
| **Horas investidas** | ~Xh |
| **Status** | ✅ Concluído |
| **Próximo** | M1.2 — Tema + Web Content + Fragments |

## O que foi entregue

- README + ROADMAP do projeto
- ADR-001 (workspace nativo no M1)
- Workspace Liferay 7.4 GA132 inicializado
- Liferay rodando em http://localhost:8081
- Admin configurado e logado no Control Panel
- Site "Hotel Livingstone" criado com 6 pages (Home, Quartos, Restaurante, Eventos, Sobre, Contato)
- Multi-idioma PT-BR/EN-US configurado
- Navigation menu "Principal" com 6 items + Primary Navigation

## Decisões tomadas (links)

- [ADR-001](../adr/ADR-001-workspace-nativo-no-m1.md) — workspace nativo no M1, Docker no M2

## O que aprendi (resumo do learning-log)

(consolidar bullets dos `docs/learning-log/*.md` da fase)

## Dificuldades encontradas

(preencher com problemas reais que aconteceram — não inventar)

## Pendências carregadas pra M1.2

- Site configurado via UI não está versionado (decisão pra M2: Site Initializer)
- Edição de `server.xml` é manual após `blade server init` — documentado no README

## Próximos passos (M1.2)

- Tema customizado (logo, cores, tipografia do Hotel Livingstone)
- Web Content Structures + Templates (Quarto, Pacote)
- Fragments customizados (Hero, Card de Quarto, Galeria)
- Popular conteúdo de exemplo (10+ quartos)

## Auto-avaliação

- **Critérios "ficou bom" atingidos?** (avaliar honestamente: você consegue explicar tudo sem consultar Claude?)
- **Práticas pilares aplicadas?** (doc-first ✓, ADRs ✓, learning-log ✓, commits PT-BR convencionais ✓)
```

- [ ] **Step 3: Atualizar ROADMAP**

Em `ROADMAP.md`, marcar M1.1 inteiro como `✅` e atualizar status de M1 pra "Em execução (M1.2 próximo)".

- [ ] **Step 4: Commit final do M1.1**

```bash
git -C ~/Documents/SEA/hotel-livingstone-onboarding add ROADMAP.md docs/retrospectivas/M1.1-setup-site-base.md
git -C ~/Documents/SEA/hotel-livingstone-onboarding commit -m "docs: retrospectiva M1.1 (setup + site base concluído)"
```

- [ ] **Step 5: Tag git pra marcar checkpoint**

```bash
git -C ~/Documents/SEA/hotel-livingstone-onboarding tag -a m1.1-done -m "M1.1: setup + site base concluído (6 pages, navigation, i18n)"
```

Validar:
```bash
git -C ~/Documents/SEA/hotel-livingstone-onboarding tag -l
```
Esperado: `m1.1-done`.

- [ ] **Step 6: (Opcional) Push pro GitHub se já criou o remote**

```bash
git -C ~/Documents/SEA/hotel-livingstone-onboarding push origin main --tags
```

---

## Critérios de conclusão do M1.1

Antes de marcar M1.1 como `✅` definitivo, validar:

- [ ] Liferay sobe em http://localhost:8081 com 1 comando após reinício do laptop (`blade server start`)
- [ ] 6 pages do site Hotel Livingstone estão acessíveis e navegáveis
- [ ] URLs `/pt-BR/web/hotel-livingstone` e `/en-US/web/hotel-livingstone` retornam 200
- [ ] Navigation menu "Principal" aparece no topo com 6 items clicáveis
- [ ] README explica o projeto pra alguém de fora
- [ ] ROADMAP mostra estado real (sem mentiras)
- [ ] ADR-001 está completo e defensável
- [ ] Pelo menos 3 entradas em `docs/learning-log/` cobrindo a fase
- [ ] Retrospectiva M1.1 escrita
- [ ] Tag `m1.1-done` criada
- [ ] **Você consegue explicar TODAS as decisões sem consultar Claude**

---

## Próximo plan (não-incluído aqui)

**M1.2 — Tema + Web Content + Fragments** será planejado depois de M1.1 concluído. Vai cobrir:
- Tema customizado do Hotel Livingstone (npm-based theme template)
- Customização visual (logo, cores, tipografia)
- Web Content Structures + Templates (estrutura de Quarto, Pacote)
- Fragments customizados (Hero, Card de Quarto, Galeria)
- Popular conteúdo de exemplo
- ADRs adicionais (002-004)
