# Learning Log — M1.2 Fase 1 (Tema skeleton)

**Data:** 2026-04-30
**Branch:** `feat/m1.2-tema-content-fragments`
**Commits:** `295dc0b` → `0d2cc7c` → `25c9edb` → `2030a1a` → `e05fd02` (5 commits sobre o plan `1e35103`)
**Estado final:** 12/12 pages HTTP 200, 4/4 classes `hl-*` em todas, 3/3 assets servindo, tema visualmente Dracula+Apple aplicado nas 6 pages.

---

## O que tentei

1. Scaffold do tema via `blade create -t js-theme` (caminho oficial 7.4 — Decisão #1 do plan).
2. Tokens CSS Custom Properties em `:root` antes dos fragments (decoupling estrutura/aparência).
3. Customização do `portal_normal.ftl` com HTML5 semântico (`<header>/<main>/<footer>` + `role="banner|main|contentinfo"`), preservando 5 elementos críticos do herdado (`<#include init />`, `top_head_include`, `quick-access`, `control_menu`, `content_include`).
4. Renderização do menu Primary via iteração direta sobre `nav_items` (alternativa: Navigation Menu Widget — descartada por menos controle de HTML).
5. Logo Dracula gerado por IA, convertido pra SVG, extraído como PNG real e otimizado.
6. Footer minimalista 3 zonas (brand / nav links / copyright) com CSS Grid Areas.

## O que errei

1. **`themeDisplay.getURLHome()` retorna URL com `/web/guest/` (HTTP 404)** — o internal `groupName` do Default Site persiste como "guest" mesmo após renomear pra "Hotel Livingstone" na UI. Detectado quando o footer linkou pra `/web/guest/sobre` e bateu 404. Fix: URLs relativas (`/sobre`, `/contato`) — funcionam pra short URLs no Default Site, mas pendência conhecida pra M2 multi-site.
2. **Conversor PNG→SVG online "fake"** — o conversor que usei wrappa o PNG raster como `<image href="data:image/png;base64,...">` dentro de uma tag `<svg>`. Resultado: 1.5MB de PNG codificado em base64, sem benefício vetorial real. Fix: extrair o PNG via `sed | base64 -d`, redimensionar com `sips -Z 256` (resultado: 1.1MB → 59KB, 95% redução).
3. **Esqueci primeiro do auto_env do SDKMAN** — `sdkman_auto_env` só dispara na pasta exata onde o `.sdkmanrc` mora. Fui direto pra `workspace/` e pegou Java 11 (default global GDF) em vez do 21 do projeto. Fix: passar pela raiz do projeto antes de `cd workspace`.
4. **Conflito de Elasticsearch sidecar com GDF** — Liferay carrega ES sidecar interno na porta 9200/9201; ambos GDF e Hotel querem essas portas. Resultado: `BindException: Address already in use`. Fix: parar o Liferay GDF antes de subir Hotel.

## O que firmou

1. **Generator-liferay-theme 10.2.0 não pede prompts interativos** — gera com defaults; `blade create -p X X` pré-resolve. Lista de templates em GA132: `js-theme` (5), `theme` legacy (25), `theme-contributor` (26).
2. **Em workspace, o fluxo Gulp é desnecessário** — Gradle plugin do workspace constrói/deploya tudo. `gulp init` skip; `liferay-theme.json` no `.gitignore` do scaffold.
3. **Yeoman não gera `src/templates/`** — FTL templates são herdados do parent automaticamente (Liferay resolve em runtime). `build/templates/portal_normal.ftl` (gerado pelo build) é a versão herdada concreta — fonte mais confiável pra customizar a partir.
4. **`<#include init />` é essencial** — popula `nav_items`, `themeDisplay`, `is_signed_in`, `is_setup_complete` etc no contexto FTL. Não pode tirar.
5. **`nav_items` aponta automaticamente pro Navigation Menu marcado como Primary** — não precisa buscar por nome. Configuração feita na Task 8 do M1.1 entrou em produção sem código adicional.
6. **`aria-current="page"` precisa ser setado manualmente via `nav_item.isSelected()`** — não vem automático. Render correto confirmado: `<a href="..." aria-current="page">Home</a>` quando estamos na Home.
7. **CSS Custom Properties (runtime) > SCSS Variables (compile-time)** pro projeto — fragments podem sobrescrever em scope local sem rebuild; Dark Mode toggle no M1.3 funciona em ms; JS pode ler/setar tokens.
8. **CSS Grid Areas é mais legível que Flexbox pra layouts 2D** — string `"brand links" / "copyright copyright"` é o layout em ASCII, auto-documentado.

## Dúvidas abertas (não-bloqueadoras)

1. **Qual é a API correta pra montar URL pra outra page do site corrente?** `getURLHome()` retorna URL inválida em Default Site renomeado. `getPathContext() + getLayoutSet().getFriendlyURL()` é confiável? Vai precisar pesquisar antes do M2 multi-site.
2. **`themeDisplay.getLayoutFriendlyURL(layout)` funciona pra iterar pages e gerar URLs corretas?** — alternativa pra footer dinâmico (em vez de hardcode `/sobre`/`/contato`).
3. **Conversor PNG→SVG vetorial real (não fake)?** — pra ter logo escalável. Inkscape `Trace Bitmap`, vectorizer.io ou autotracer.org. Validar qualidade no logo Dracula antes de adotar (gradients e detalhes finos podem ficar feios em vetorial low-poly).

## Pendências técnicas pra Fase 5 / M1.3

1. **`workspace/.yarnrc`, `workspace/package.json`, `workspace/yarn.lock` untracked** — Liferay Gradle plugin gera ao buildar tema (cache yarn pra deps frontend). Adicionar ao `.gitignore` raiz do projeto.
2. **URLs relativas no footer vão linkar errado em multi-site (M2)** — quando vier multi-site, substituir `/sobre`/`/contato` por construção dinâmica via `themeDisplay.getLayoutFriendlyURL()` ou `themeDisplay.getPathContext() + groupFriendlyURL + pageFriendlyURL`.

## Próximos passos

**Fase 2 — Web Content Structure "Quarto":**
- Task 8: Structure "Quarto" (6 campos: title, subtitle, description, photo, price, amenities)
- Task 9: Template "Quarto" (FTL)
- Task 10: 3 Articles (Suíte Kilimanjaro / Serengeti / Okavango — preciso pensar se mantenho nomes safari ou troco pra vibe Dracula tipo "Suíte Carmilla / Lestat / Drácula")
